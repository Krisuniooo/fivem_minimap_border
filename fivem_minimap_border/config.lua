CONFIG = {}

CONFIG.HIDE_STATUS = true -- Set to false if any other script handles it already (for optimization)

CONFIG.DISPLAY_N_BLIP = false -- Display North on Minimap
CONFIG.DISPLAY_IN_CAR = false -- Display while in car (compatible with: ESX or baseevents)
CONFIG.DISPLAY_ON_PAUSEMENU = false -- Display during pause menu (works only if CONFIG.DISPLAY_IN_CAR is set to false and works only for new version of ESX)
CONFIG.DISPLAY_ON_OXINVENTORY = false -- Display while ox_inventory is opened (works only with newer versions of ox_inventory)